/**
 * Movie Details Page (MDP)
 *
 * MDP is a canvas containing a menu of actions, e.g. Play, Add to My List, that
 * can be performed on a title.
 *
 * @tag mdp
 */

import bob from '../components/bob';
import menu from '../components/menu';

export default {};
