/**
 * Search
 *
 * Search is a canvas allowing members to use a keyboard to input titles to
 * find. Boxshots of results are shown in a gallery.
 *
 * @tag search
 */

import boxshot from '../components/boxshot';
import keyboard from '../components/keyboard';
import menu from '../components/menu';

export default {};
